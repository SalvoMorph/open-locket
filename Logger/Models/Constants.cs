namespace Sandbox.Models // change with the correct nampespace
{
    /// <summary>
    /// Log Target
    /// </summary>
    public enum LogTarget
    {
        File,
        Console,
        Both
    }

    /// <summary>
    /// Log Type
    /// </summary>
    public enum LogType
    {
        INFO,
        WARNING,
        ERROR
    }
}
